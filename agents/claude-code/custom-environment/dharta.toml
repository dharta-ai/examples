# Custom environment example: install a dependency before the agent runs, then use it.
# Docs: https://docs.dharta.sh/build/dharta-toml

name = "custom-environment"
deploy = true

[harness]
type = "claude-code"

# Dharta runs setup.sh once when this agent's workspace is first prepared, before its
# first turn. Installs land in the agent's home, so they persist with the workspace and
# are available on every turn. (Dharta routes `pip` / `npm` installs to the home for you.)
[environment]
setup = "setup.sh"
